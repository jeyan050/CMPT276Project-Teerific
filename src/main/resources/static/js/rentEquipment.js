// Connect to database?
// var pg = require('pg');
// var connection = "jdbc:postgresql://localhost/postgres?user=postgres&password=root";
// var pgClient = new pg.Client(connection);
// pgClient.connect();

// var query = pgClient.query("SELECT stock FROM inventory");

// var labels = document.getElementsByClassName("label");
// var inputs = document.getElementsByClassName("input");

// inputs.addEventListener('input', function() {

// })